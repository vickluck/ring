syntax = "proto3";

package provider;

service Provider {
	rpc Registry(Request) returns(Response){}
}


message Request {
	string msrv_name = 1;
	string ip = 2;
	int32 port = 3;
	string id = 4;
}

message Response {
}
